# List any packages that need updating
up2date --list

# Seach packages
rqm -qa | grep foo          # Search for a package already installed
up2date --showall           # List all packages available for download
up2date --show-available    # Show all packages available that aren't installed

# Install package
up2date <package-name>

# List package contents
rpm -lpq <package-name>
