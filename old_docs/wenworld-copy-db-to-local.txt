$ ssh titan
$ pg_dump -U example example > example.sql

Open a localhost tab
$ scp titan:example.sql .
$ dropdb example
$ createdb example -O example
$ psql -U example -d example < example.sql
$ rm example.sql

Switch to the remote host tab
$ rm example.sql
